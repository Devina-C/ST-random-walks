# ROI extraction

Region of interest (ROI) coordinate transformation. ROI polygons were originally defined in Xenium Explorer and exported as raw pixel coordinates, then converted to GeoJSON for downstream spatial masking.

## Contents

- **`scripts/transform.py`** - converts raw ROI coordinates exported from Xenium Explorer (CSV) into standardised GeoJSON polygons used for spatial filtering in downstream analysis.

